@model LibraryManagementSystem.Models.Book

@{
    ViewData["Title"] = "Detail";
    var countryNames = new Dictionary<string, string>
    {
        { "00", "Không rõ" },
        { "VN", "Tiếng Việt" },
        { "EN", "Anh" },
        { "US", "Hoa Kỳ" },
        { "FR", "Pháp" },
        { "JP", "Nhật Bản" },
        { "KR", "Hàn Quốc" },
        { "CN", "Trung Quốc" },
        { "DE", "Đức" },
        { "AU", "Úc" },
        { "IT", "Ý" },
        { "ES", "Tây Ban Nha" },
        { "CA", "Canada" },
        { "IN", "Ấn Độ" },
        { "BR", "Brazil" }
    };
}

<link rel="stylesheet" href="~/features/Book/Detail/book.css?v=@DateTime.Now.Ticks" />

<div class="book-detail">
    <!-- Left Section -->
    <div class="book-detail-left">
        <div class="book-detail-left-img">
            <!-- Hiển thị hình ảnh lớn trong một div riêng biệt với chiều cao và chiều rộng cố định -->
            <div class="large-image-container">
                <img id="largeImage"
                     src="data:image/jpeg;base64,@Convert.ToBase64String(Model.BookImgs.FirstOrDefault()?.Image)"
                     alt="Book Image"
                     class="large-image" />
            </div>
        </div>

        <!-- Hiển thị tất cả các hình ảnh của sách -->
        <div class="book-images-gallery">
            @foreach (var img in Model.BookImgs)
            {
                <div class="book-image">
                    <img src="data:image/jpeg;base64,@Convert.ToBase64String(img.Image)"
                         alt="Book Image"
                         class="small-image"
                         onclick="changeLargeImage('@Convert.ToBase64String(img.Image)')" />
                </div>
            }
        </div>
    </div>

    <!-- Right Section -->
    <div class="book-detail-right">
        <!-- Book Title and Authors -->
        <div class="book-title-authors">
            <h1>@Html.DisplayFor(model => model.Name)</h1>
            <p class="authors">
                <span><b>Tác giả: </b></span>
                @if (Model.Authors.Any())
                {
                    var isFirst = true;
                    foreach (var author in Model.Authors)
                    {
                        if (!isFirst)
                        {
                            @:<span>, </span>
                        }
                        <a>@author.Name</a>
                        isFirst = false;
                    }
                }
                else
                {
                    <span>Chưa có tác giả</span>
                }
            </p>
        </div>

        <!-- Description -->
        <div class="book-description">
            <p class="info-title"><b>Mô tả:</b></p>
            <p>@Html.DisplayFor(model => model.Description)</p>
        </div>

        <!-- Basic Info -->
        <div class="book-basic-info">
            <div>
                <p class="info-title">Ngày phát hành:</p>
                <p>@Html.DisplayFor(model => model.PublishYear)</p>
            </div>
            <div>
                <p class="info-title">Nhà phát hành:</p>
                <p>@Html.DisplayFor(model => model.PublisherNavigation.Name)</p>
            </div>
            <div>
                <p class="info-title">Ngôn ngữ:</p>
                <p>@(countryNames.ContainsKey(Model.Language) ? countryNames[Model.Language] : "Không rõ")</p>
            </div>
            <div>
                <p class="info-title">Số trang:</p>
                <p>@Html.DisplayFor(model => model.PageNumber)</p>
            </div>
        </div>

        <!-- Categories -->
        <div class="book-categories">
            <p>
                <b>Thể loại:</b>
                @if (Model.Categories.Any())
                {
                    var isFirstCateg = true;
                    foreach (var categ in Model.Categories)
                    {
                        if (!isFirstCateg)
                        {
                            @:<span>, </span>
                        }
                        <a>@categ.Name</a>
                        isFirstCateg = false;
                    }
                }
                else
                {
                    <span>Chưa có thể loại</span>
                }
            </p>
        </div>

        <!-- Additional Info -->
        <div class="book-additional-info">
            <div>
                <p><b>Thuộc series:</b> @Html.DisplayFor(model => model.SeriesNavigation.Name)</p>
            </div>
            <div>
                <p><b>Mã ISBN:</b> @Html.DisplayFor(model => model.Isbn)</p>
            </div>
            <div>
                <p><b>Phiên bản:</b> @Html.DisplayFor(model => model.Version)</p>
            </div>
            @if (User.IsInRole("ADMINISTRATOR"))
            {
                <div>
                    <p>
                        <b>Nhà cung cấp:</b>
                        <a asp-action="DetailVendor" asp-controller="Vendor" asp-route-id="@Model.Vendor">@Model.VendorNavigation.Name</a>
                    </p>
                </div>
            }
        </div>

        <!-- Update Button for Admin -->
        @if (User.IsInRole("ADMINISTRATOR"))
        {
            <a asp-action="Update" asp-route-id="@Model.Id" class="btn btn-primary">Cập nhật</a>
        }
    </div>
</div>

<script>
    // Function to change the large image when a small image is clicked
    function changeLargeImage(imageBase64) {
        var largeImage = document.getElementById("largeImage");
        largeImage.src = "data:image/jpeg;base64," + imageBase64;
    }
</script>
